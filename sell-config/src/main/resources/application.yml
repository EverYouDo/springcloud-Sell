spring:
  application:
    name: sell-config
  cloud:
    config:
      server:
        git:
          # Git仓库地址
          uri: https://github.com/Guiee/spring-cloud-config-repo.git
#          basedir: D:\IdeaProjects\basedir
  rabbitmq:
    host: 192.168.246.134
eureka:
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
